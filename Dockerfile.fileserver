FROM python:3.12-slim
RUN apt-get update && apt-get install -y --no-install-recommends curl && rm -rf /var/lib/apt/lists/*
WORKDIR /app
COPY src/services/file_server.py .
EXPOSE 9900
CMD ["python", "file_server.py", "--port", "9900", "--roots", "/mnt", "/home"]
